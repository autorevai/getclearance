{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python -c \"from sqlalchemy import create_engine, text; import os; e=create_engine(os.environ['DATABASE_URL'].replace('+asyncpg','')); c=e.connect(); c.execute(text('DELETE FROM alembic_version')); c.execute(text(\\\"INSERT INTO alembic_version (version_num) VALUES ('004')\\\")); c.commit(); c.close(); print('Fixed alembic version')\" && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
