# SignalWeave Backend Requirements
# Updated: 2025-11-30
# Python 3.11+

# ============================================
# EXISTING DEPENDENCIES (from your current setup)
# ============================================

# FastAPI framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
alembic==1.13.1
asyncpg==0.29.0  # Async PostgreSQL driver
psycopg2-binary==2.9.9  # For Alembic migrations

# Redis
redis==5.0.1
hiredis==2.3.2  # Faster Redis protocol

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Pydantic & Config
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP Client
httpx==0.26.0

# Utilities
python-dateutil==2.8.2

# ============================================
# NEW DEPENDENCIES (for Sumsub features)
# ============================================

# AI Integration
anthropic==0.18.1  # Claude API client

# Storage (Cloudflare R2 / AWS S3)
boto3==1.34.34  # AWS SDK for S3-compatible storage
botocore==1.34.34

# OCR (Phase 3)
# Uncomment when implementing OCR service:
# google-cloud-vision==3.7.0  # Google Vision OCR
# boto3 (already listed above for AWS Textract)

# Fuzzy Matching for Screening
python-Levenshtein==0.25.0  # Fast string distance calculations
fuzzywuzzy==0.18.0  # Additional fuzzy matching utilities

# Background Workers (ARQ)
arq==0.25.0  # Redis-based async task queue
watchfiles==0.21.0  # For ARQ file watching

# Validation & Utilities
phonenumbers==8.13.27  # Phone number validation
pycountry==23.12.11  # Country code validation
email-validator==2.1.0  # Email validation

# PDF Generation (Phase 5 - Evidence Export)
reportlab==4.0.9  # PDF generation
PyPDF2==3.0.1  # PDF manipulation

# Image Processing (for document quality checks)
Pillow==10.2.0  # Image manipulation

# ============================================
# DEVELOPMENT DEPENDENCIES
# ============================================

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0  # For testing API endpoints (already listed above)

# Code Quality
black==24.1.1  # Code formatting
ruff==0.1.14  # Linting
mypy==1.8.0  # Type checking

# Security
bandit==1.7.6  # Security linting

# ============================================
# PRODUCTION DEPENDENCIES
# ============================================

# Monitoring
sentry-sdk[fastapi]==1.40.0  # Error tracking

# Performance
gunicorn==21.2.0  # Production WSGI server
